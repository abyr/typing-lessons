Lesson 23: symbols
func(a, b);
def abc:
$a = 0; $b = 1;
&$x; &$y;
a^2; b^3; 2^2;
(a, b) => return {a + b};
(x, y) => return x - y;
void: abc;
((a, b) => (++a++ % ++b++));
((x, y) => (--x-- / --y--));
for (var i=0; i<10;++i);
if (a === b && x <= y && i % 10) { i++ };
++--__a; --++__b;
(( a * b * c ) * ( x * y * z ) * i )
(x/y/z / a/b/c)
